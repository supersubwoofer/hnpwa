{
  "name": "hnpwa",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:dev": "rm -rf dist",
    "clean:build": "rm -rf build",
    "clean:all": "npm run clean:dev & npm run clean:build",
    "build": "gulp",
    "dev": "gulp && concurrently --kill-others \"gulp watch\" \"npx serve build\"",
    "lint": "tslint -c tslint.json --project tsconfig.json src/**/*.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "idb": "^4.0.4",
    "lit-element": "^2.2.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "del": "^5.0.0",
    "gulp": "^4.0.2",
    "gulp-typescript": "^5.0.1",
    "tslint": "^5.18.0",
    "tslint-config-airbnb": "^5.11.1",
    "typescript": "^3.5.2",
    "workbox-build": "^4.3.1"
  },
  "browserslist": [
    "> 5%"
  ]
}
